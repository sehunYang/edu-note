# 업데이트

새 기능이 나오거나 문제가 고쳐지면, 선생님 저장소에 **업데이트 Pull Request**가
자동으로 올라옵니다. 할 일은 **[Merge] 한 번**입니다.

---

## 어떻게 오나요

주 1회, GitHub이 원본을 확인해 새 버전이 있으면 PR을 만들어 둡니다.
GitHub 알림 메일을 받으시거나, 저장소의 **Pull requests** 탭에서 확인할 수 있습니다.

앱 안 **세팅실 → 시스템 상태**에서도 현재 버전을 볼 수 있습니다.

## 반영하는 법

1. 저장소 → **Pull requests** 탭
2. `Edu_Note 업데이트 (날짜)` 열기
3. 내용을 확인하고 **Merge pull request** → **Confirm merge**
4. 몇 분 뒤 Vercel이 새 버전을 배포합니다

**데이터베이스 변경이 있으면 배포 중에 자동으로 적용됩니다.** 따로 할 일이 없습니다.
기존 데이터는 그대로 유지됩니다.

---

## ⚠️ 데이터가 바뀌는 변경

PR 본문에 **"데이터가 바뀌는 변경이 포함돼 있습니다"** 경고가 있으면,
머지 전에 **세팅실 → 시스템 상태 → 백업 내려받기**로 백업해 두세요.

이런 변경은 되돌리기 어렵습니다. 릴리스 노트에 무엇이 바뀌는지 적어 두겠습니다.

---

## PR이 안 만들어질 때

저장소 **Settings → Actions → General** 에서
**Allow GitHub Actions to create and approve pull requests** 를 켜 주세요(한 번만).

이 설정이 꺼져 있으면 브랜치(`update/날짜`)까지만 만들어집니다.
그 경우 저장소 상단의 **Compare & pull request** 버튼으로 직접 열면 됩니다.

## 충돌이 났을 때

코드를 직접 고치신 적이 있으면 자동 병합이 실패하고 **Issue**가 하나 생깁니다.
그 내용을 그대로 복사해 [원본 저장소 Issues](https://github.com/sehunYang/edu-note/issues)에
남겨 주시면 도와드리겠습니다.

## 업데이트를 안 받고 싶다면

저장소 → **Actions** 탭 → `업데이트 확인` 워크플로 → **Disable workflow**.

다만 학교 시스템(나이스·컴시간)의 형식이 바뀌면 앱이 멈출 수 있고,
그 수정도 업데이트로 전달되므로 켜 두시길 권합니다.
