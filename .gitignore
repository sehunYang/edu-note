# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (비밀키 — 절대 커밋 금지)
.env
.env*.local
.env.production

# vercel
.vercel

# typescript
*.tsbuildinfo

# OMC 상태 (로컬 전용)
/.omc/state/

.vercel
.env*

# Playwright MCP 산출물 (페이지 스냅샷에 학생 실명이 들어감 — 절대 커밋 금지)
/.playwright-mcp/

# OMC 훅이 하위 디렉터리에 남기는 상태 파일 (공개 리포에 들어가면 안 되는 잡음)
**/.omc/state/

# 개발 부산물 — 공개 저장소에 들어갈 이유가 없다 (배포판 S7)
/.omc/
/.codex/
/report/
/qc-report-*.md

# Claude Code 개인 권한 설정 (로컬 전용)
.claude/settings.local.json
